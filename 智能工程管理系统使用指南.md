下面是当前工程管理系统的推荐使用流程。

## 1. 进入工作区

在 Codex 中打开：

```
D:\工程
```

后续所有项目都放在该目录下，目录名统一为：

```
<芯片平台>_<项目或机型名称>_<需求>
```

例如：

```
axera_Z5_某机型_升级异常
```

## 2. 创建新项目

直接告诉我：

```
创建项目：
芯片平台：axera_Z5
项目/机型：某机型
需求：升级异常
需求详情：……
验收条件：……
SDK 路径/分支：……
Git 仓库：……
硬件资料：……
硬件修改记录：……
修改后指标：……
```

远程服务器已经有默认配置，通常只需补充项目对应的 SDK 路径、Git 工作区和分支。

系统会先检查：

- 项目名称是否符合规范；
- SDK、Git 和编译服务器配置；
- 硬件资料是否完整；
- 是否需要设备调试；
- 是否需要编译镜像；
- 是否需要多个对话协作。

## 3. 先进行对话拆分评审

在正式开发前，系统会先划分子项目，例如：

1. 需求与硬件资料分析；
2. 源码修改；
3. 编译和镜像合法性验证；
4. 设备串口测试；
5. 项目收尾和文档整理。

系统会为每个阶段建议对话数量，并记录：

- 对话目标；
- 输入资料；
- 负责范围；
- 输出文件；
- 交接摘要；
- 下一步入口。

只有你确认后，才创建或拆分新的 Codex 对话。

## 4. 借鉴旧项目

如果新需求可能和历史项目有关，可以说：

```
查找以前是否做过类似的升级异常处理，重点查找修改过的源码路径和测试结果。
```

系统会先查统一索引，只加载命中的项目和文件，不会批量读取所有历史项目。

## 5. 打开或继续已有项目

可以说：

```
打开 axera_Z32.0_Z3移植到Z5 项目。
```

或者：

```
继续 nova_X3N3.0_升级异常 项目，先告诉我当前状态。
```

系统只读取：

- `.project/project.json`
- `.project/SUMMARY.md`
- 最近的构建、测试和源码变更记录
- 必要的健康检查结果

打开项目本身是只读操作，不会自动修改远程源码。

## 6. 开始源码开发

当你明确说：

```
开始修改源码，解决这个问题。
```

系统会先：

- 核对远程 SDK 当前分支、提交和工作区状态；
- 检查是否有其他任务占用共享 SDK；
- 获取对应的工作区租约；
- 确认修改范围和风险；
- 只加载当前子项目需要的资料。

共享 SDK 默认复用，不会为每个项目创建独立 worktree。

## 7. 记录源码变更

每个逻辑更新批次都要同步：

```
.project/changes.jsonl
.project/git-add-history.jsonl
docs/源码变更清单.md
```

Markdown 清单中会为每次更新生成独立卡片，包含：

- 秒级更新时间；
- 本批次修改的全部文件；
- 修改说明；
- 聚合后的 `git add 文件名` 命令。

系统只生成和展示 `git add`，不会未经确认自动暂存源码。

## 8. 远程编译和镜像归档

编译前必须确认：

- 芯片平台和机型；
- 硬件版本；
- 存储类型；
- 分区表；
- 分区容量；
- 镜像大小规则；
- `magic_hex` 或 SHA-256；
- 安全启动和回滚策略。

编译完成后，只把需要交付或回溯的镜像复制到：

```
项目目录/artifacts/images/
```

禁止创建项目根目录下独立的 `images/`。

同时记录：

- 远程镜像路径；
- 项目相对路径；
- 源码分支和提交；
- SHA-256；
- 镜像大小和合法性检查；
- 是否允许烧录。

校验未通过时，不复制、不烧录、不标记构建成功。

## 9. 串口调试设备

你可以说：

```
通过串口调试当前开发板。
```

系统会先确认：

- 设备当前处于 U-Boot、Linux 还是升级模式；
- 串口端口；
- 波特率；
- 登录方式；
- 当前故障现象；
- 是否允许重启、烧录或修改设备。

密码只在当前对话中使用，不写入项目文件。

## 10. 项目完成和交付

当你说：

```
项目完成，请整理并收尾。
```

系统会执行：

- 核对实际 Git 变更路径；
- 生成源码变更清单；
- 汇总软件修改设计；
- 整理编译、测试和硬件修改记录；
- 核对镜像大小、结构和哈希；
- 总结最终修改的源码路径；
- 提取可复用经验；
- 列出未完成事项；
- 更新项目状态；
- 刷新当前项目的全局索引。

最终会告诉你：

- 修改了哪些源码文件；
- 每个文件修改了什么；
- 使用了哪个分支和提交；
- 生成了哪些镜像；
- 镜像 SHA-256；
- 测试是否通过；
- 是否还有风险或待办。

## 11. 管理系统本身的版本控制

管理仓库：

```
https://github.com/xyq99/Engineering_Management
```

主要保存：

- 全局规则；
- skills；
- 项目模板；
- 元数据结构；
- 项目索引。

不会保存：

- SDK 源码；
- 编译输出；
- 镜像文件；
- 明文密码；
- 私钥和 Token。

一句话概括：

```
提出需求 → 收集配置 → 评审对话拆分 → 创建/打开项目
→ 查历史经验 → 获取 SDK 租约 → 修改源码
→ 记录变更 → 远程编译 → 校验并归档镜像
→ 串口测试 → 收尾总结 → 刷新索引
```

## 12. 工程管理系统

### 12.1 创建项目并开始

```
创建并开始开发项目：
axera_Z5_2GB-DDR适配。硬件资料在……；硬件具体更改内容
```

### 12.2 提取临时会话并总结

```
请归档当前会话到所属项目，但不要把项目标记为 completed。

提取并回写：
1. 本次需求和关键决策
2. 已修改或计划修改的源码路径
3. 编译、测试和实测指标
4. 硬件修改及相关资料
5. 已验证的可复用经验
6. 未完成事项和下一步

更新对应的 .project 结构化记录和 SUMMARY.md，
然后刷新项目索引。
不要保存密码，不要记录未经验证的推测。
```

### 12.3  汇报当前的项目的进度

```
打开当前已有项目。

先读取：
1. .project/SUMMARY.md
2. .project/project.json
3. 未完成事项和结构化工程记录

不要批量加载其他项目，也不要立即访问远程 SDK。
先向我汇报项目状态、已有成果、待完成事项和建议的下一步。
```

### 12.4 其他平台的agent介入该系统

```
接管当前嵌入式项目。

请先读取：
1. AGENTS.md
2. .project/project.json
3. .project/SUMMARY.md
4. 相关结构化工程记录

然后使用 $check-engineering-workspace 检查当前状态。
不要批量加载其他项目，不要立即修改远程 SDK；
先汇报项目现状、已有成果、风险和未完成事项。
```

### 12.5 项目目录文件整理

- 直接输入：

```
整理当前项目目录，先只生成目录整理预览，不移动任何文件。
```

- 确认预览后，再输入：

```
执行已经确认的目录整理计划，只处理预览中列出的文件。
```

